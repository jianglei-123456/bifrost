# Bifrost 操作手册 · 常见问题

| 项目 | 内容 |
| --- | --- |
| 文档版本 | 1.0.0 |
| 更新日期 | 2026-09-10 |
| 文档状态 | 定稿 |
| 关联文档 | [00-总览](00-总览.md)、[01-安装与启动](01-安装与启动.md)、[02-连接Subsonic客户端](02-连接Subsonic客户端.md)、[04-连接阅读器客户端](04-连接阅读器客户端.md)、《Subsonic_API_参考》§3 |

---

## 1. 排查顺序（先看这条）

遇到任何"连不上"问题，按顺序自查：

1. **服务在跑吗**（Bifrost 机器上）：`curl http://localhost:18080/api/ping` → `pong`？
2. **端口通吗**（客户端设备上）：浏览器打开 `http://192.168.1.10:18080/api/ping` 有结果？
3. **认证对吗**：`curl "http://localhost:18080/rest/ping.view?u=admin&p=你的密码&v=1.16.1&c=test"` → `status="ok"`？
4. **客户端字段对吗**：对照 [02](02-连接Subsonic客户端.md) §4 的填写清单。
5. **客户端到底请求了哪个 URL**（客户端某个页面"没有数据"时最快的定位手段）：后端默认打印**每个**请求的原样 URL，
   见下方 §1.1。

第 1、2 步不过 → 看 §2；第 3 步不过 → 看 §3；只有第 4 步不过 → 看 [02](02-连接Subsonic客户端.md) §2/§4。**电子书 / OPDS（阅读器连不上、404、书库为空）问题直接看 §10。**

### 1.1 看客户端请求日志（页面没数据时先看这里）

每个进来的请求打两行：**请求行**（在认证、路由、参数校验**之前**打印，所以认证失败、端点未实现的请求也在）
与**响应行**（HTTP 状态码 + 耗时）。日志与业务日志同一个文件：`${bifrost.data.dir}/logs/bifrost.log`
（默认 `./data/logs/bifrost.log`；容器里是 `/data/logs/bifrost.log`，`docker logs` 也能看到同样内容）。

```text
--> GET /rest/getAlbumList2.view?u=admin&p=***&v=1.16.1&c=DSub&f=json&type=random&size=10 from 192.168.1.10
<-- 200 GET /rest/getAlbumList2.view (12 ms)
```

- `c=` 是**客户端名**（哪个 App 发的），`f=` 是要 XML 还是 JSON；`u/v/t/s/p` 是协议认证参数。
- 凭据参数 `p`（明文口令）、`t`（令牌）、`s`（盐）一律记为 `***`，其余参数**原样**保留（编码也不动，便于原样复现）。
- 表单 POST（如 `createPlaylist`、`scrobble`）的参数在请求体里，日志同样会列出。
- Subsonic 协议失败也是 **HTTP 200**（错误在响应信封的 `status="failed"` 里），所以响应行只反映传输层；
  端点是否实现要拿 URL 对照《音乐管理功能说明》§8.2 能力矩阵——**未实现端点走兜底返回 `error code="0"`**（见 §4）。
- 嫌吵：`bifrost.request-log.enabled=false`（环境变量 `BIFROST_REQUEST_LOG_ENABLED=false`）关闭。

> 想看**更底层**的（连 Tomcat 都没进来的、被容器直接拒掉的请求）可以另开 Tomcat 访问日志：
> `server.tomcat.accesslog.enabled=true` + `directory` / `pattern: '%t %a "%r" %s %b %D'`——注意 `%r` 含完整
> 查询串且**不打码**（口令会明文落盘），排障完记得关。

## 2. 连不上 / 超时 / 拒绝连接

| 现象 | 原因与处理 |
| --- | --- |
| 本机能访问，手机/其它设备不行 | 地址用了 `localhost`（指设备自己）；改填 Bifrost 主机局域网 IP（[02](02-连接Subsonic客户端.md) §2.1） |
| 局域网内也无法访问 | ① 防火墙未放行 18080 端口；② Bifrost 只监听了回环地址——确认应用配置的是 `0.0.0.0` 或未限制绑定地址；③ 路由器 AP 隔离（访客网络） |
| 端口不通但应用在跑 | `server.port` 被改过；或 Docker/反向代理映射了别的端口（见 §8） |
| 能打开但一直转圈 | 地址填到了具体端点（如 `/rest/ping.view`）导致 404；只填到主机:端口（[02](02-连接Subsonic客户端.md) §2.2） |

## 3. 认证失败（error 40）

Subsonic 认证失败统一返回 `status="failed"` + `error code="40"`（协议特性，非 HTTP 401）。

| 原因 | 处理 |
| --- | --- |
| 用户名/密码拼写错误 | 用户名默认 `admin`；密码为初始密码（[01](01-安装与启动.md) §3） |
| **首次启动没设初始密码** | 未注入 `BIFROST_AUTH_INITIAL_PASSWORD` 时密码不会写入数据库，任何密码都失败。兜底见下方"密码忘记" |
| 改过密码后客户端仍用旧密码 | 所有客户端用新密码重连（[01](01-安装与启动.md) §6） |
| 密码含特殊字符被 URL 转义破坏 | 客户端应自行 URL 编码；排查时用 curl 的 `--data-urlencode` |

**密码忘记（含首次未设置）兜底**：当前无找回机制。若数据目录可牺牲，删除数据目录（默认 `./data`）后重新启动并设置初始密码——**注意：媒体索引、歌单、收藏、评分全部重置**，需重新扫描（音乐文件本身不受影响）。操作前先备份（[01](01-安装与启动.md) §7）。

## 4. 客户端报"未实现 / error 0"

Bifrost 按《音乐管理功能说明》§8.2 能力矩阵实现端点，未实现端点返回 `error code="0"`（如 Sharing/Podcast、Jukebox、转码相关）。**这不是配置错误**。列表中的"⏳ 可增强"项在后续版本逐步补齐。歌词说明：`getLyrics`/`getLyricsBySongId` 已实现，数据来自**音频文件内嵌歌词标签**（USLT/©lyr/Vorbis）；文件无内嵌歌词则客户端显示"无歌词"，在线歌词源（LRCLIB 等）未接入。

## 5. 能连上但看不到音乐 / 列表为空

1. 音乐目录是否配置且启用：`curl -u admin:密码 http://localhost:18080/api/music-roots`（[01](01-安装与启动.md) §5.2）。
2. 是否扫描过：触发 `POST /api/music-roots/scan/all`（同步，返回最终统计）后查看 `GET /api/music-roots/scan/status`；新加文件需重新扫描（增量，很快）。
3. **重新同步曲库（全量重扫）**：改了标签/想回填歌词等新字段时，触发 `POST /api/music-roots/scan/all?fullScan=true`（或单个音乐目录 `POST /api/music-roots/{id}/scan?fullScan=true`；Subsonic 端点为 `startScan` 加 `fullScan=true` 参数）。fullScan 会忽略指纹、逐文件重读标签；常规增量扫描只在文件大小/修改时间变化时重读。
   - **fullScan 还会彻底删除已消失文件的记录**（连带清理歌单条目、书签、播放队列引用）；而增量扫描只是把缺失曲目标记隐藏、保留记录。**注意**：若外置盘临时离线，请勿触发 fullScan（会把该盘曲目从库中清掉，歌单引用一并移除，不可逆）。
4. 文件格式：仅 MP3 / FLAC / M4A 参与索引聚合；WAV 可播不入聚合；WMA/APE 不支持。
5. 音乐目录禁用（enabled=false）或文件已缺失：对应曲目会对客户端隐藏（`isAvailable=false`）。

## 6. 中文/日文搜索或分组不对

- 中文按**拼音首字母**分组（如"周杰伦"→ Z），个别多音字可能不准（可增强项，暂无人工纠错表）。
- 日文/韩文/数字/符号归入 `#` 组。
- 若刚扫描完分组未刷新，重新触发扫描（分组在扫描时计算并持久化）。

## 7. 播放失败 / 无声 / 格式不支持

- Bifrost `stream` **直出原文件、不转码**（无 ffmpeg 转码链路），客户端必须支持你的音频编码（MP3/FLAC/M4A 通常都没问题；无损 FLAC 在部分低端客户端/浏览器上可能不支持）。
- 播放中断可尝试：换有线网络 / 降低客户端缓冲；Range/206 已支持，拖动进度条正常。
- 专辑/歌单整体下载（zip）为可增强项，`download` 目前支持单曲。

## 8. 反向代理 / HTTPS / 容器相关

- **反向代理**：Subsonic 请求是普通 HTTP GET，任何反代均可；把 `/rest` 与 `/api` 都代理到 18080，地址则填反代域名。注意启用 HTTPS 后再暴露公网（明文密码传输风险）。
- **路径前缀**：Bifrost 无内置 context-path，若反代加了前缀（如 `https://域名/bifrost`），客户端地址相应填 `https://域名/bifrost/rest`。
- **Docker**：1.0.0 起提供**单镜像**（管理端与后端同镜像、挂 `/admin/`），构建与部署照 [05-Docker部署](05-Docker部署.md) 的命令清单执行；容器里数据卷是 `/data`，端口映射保持 `18080:18080`（改映射会让三类客户端的连接地址与文档不一致）。

## 9. 数据备份与迁移

- 备份 = 复制整个 `./data`（SQLite 库 + 封面缓存 + 密钥），见 [01](01-安装与启动.md) §7。
- 迁移到新机器：装好 Java 21 → 拷入 `./data` → 启动（无需再设初始密码，账号在库里）→ 确认各音乐目录/图书目录的路径在新机器上仍有效，必要时改路径并重新扫描。

## 10. 电子书 / OPDS 连不上（阅读器专用）

> 阅读器连接操作见 [04-连接阅读器客户端](04-连接阅读器客户端.md)（OPDS 地址唯一正确填法是 `http://主机:18080/opds/v1.2/catalog`）。

| 现象 | 原因与处理 |
| --- | --- |
| 填 `http://主机:18080/opds`（或 `/opds/`、`/opds/v1.2`、多了尾斜杠）→ **404** | Bifrost 的 OPDS 挂在 `/opds/v1.2/` 路径下，**没有** `/opds` 快捷入口、不做自动跳转。填完整路径 `http://主机:18080/opds/v1.2/catalog`（见 [04](04-连接阅读器客户端.md) §3.1） |
| Readest 添加后一直失败/转圈 | **用的是 readest.com 网页托管版**：其同源代理带 SSRF 防护，生产环境会拦 `localhost`/局域网地址。改用 **Readest 桌面端**（原生直连，无 CORS 限制） |
| 目录能打开，但"全部图书/最近添加"是空的 | ① 没建图书目录 / 没扫描：`curl -u admin:密码 http://localhost:18080/api/book-roots` 看列表，没有就先建目录+扫描（[04](04-连接阅读器客户端.md) §1）；② 书目录里没有 EPUB（含 kepub.epub）/PDF（TXT/MOBI 不支持）；③ 扫描还在进行中（`GET /api/book-roots/scan/status` 未到 IDLE） |
| 目录有书，但某本打不开/下载失败 | ① 只支持 EPUB / PDF，其它格式不会出现在目录里；② 文件损坏或已被移动删除时该书 `isAvailable=false`，对客户端隐藏或下载 404；③ 关闭了 require-auth 却填了账号（或反之），见 [04](04-连接阅读器客户端.md) §3.3 |
| 手机/墨水屏连不上（Readest/KOReader） | 地址用了 `localhost`（指设备自己）→ 填 Bifrost 主机局域网 IP；并放行防火墙 18080 端口（见 §2） |
| 开启 require-auth 后返回 401 | OPDS 用 HTTP Basic，用户名/密码 = 管理员账号（默认 `admin`）；改过密码用新密码重连（见 [04](04-连接阅读器客户端.md) §3.3） |
| KOReader 里看不到"搜索" | OPDS 搜索入口在目录根已声明（OpenSearch），进入"全部图书/最近添加"后在页面内找搜索按钮；个别版本需先进入某个子目录才显示 |

## 11. 阅读进度同步（KOSync，M3-sync）

> 接入步骤见 [04-连接阅读器客户端](04-连接阅读器客户端.md) §7。

| 现象 | 原因与处理 |
| --- | --- |
| KOReader 里 *Progress sync* 连不上 / 一直失败 | ① 地址填错：**必须带 `http://`** 且**只填到主机:端口**（如 `http://192.168.1.10:18080`）——输入框预填的是 `https://`，不改必然失败；② 服务没起或防火墙未放行 18080（见 §2） |
| Register / Login 报错 | 同步账号或口令不对：到管理端「阅读进度」页复制**用户名 + 明文口令**（口令默认打码，点"显示"）。用户名不一致会提示"用户名已被占用"，口令不符会提示"同步账号口令不匹配"——这些都是服务端返回的原文提示 |
| 昨天还能同步，今天报认证失败（401） | 同步口令被改过（改口令后所有设备失效）。在设备上重新 Login 即可；**改口令不影响已同步的进度数据** |
| 设备 A 读了，设备 B 没跳到新位置 | ① 设备上 **"Automatically keep documents in sync" 没打开**（默认关闭）；② 客户端有 10 秒翻页防抖 + 25 秒全局去抖，且**关书/挂起时才自动推送**——手动 *Push progress from this device now* 最可靠；③ 两台设备上的书**不是同一份文件**（不同来源/重新打包 → 文档指纹不同，协议层无法匹配） |
| 管理端「阅读进度」页出现"孤儿进度" | 上报的书在库里找不到对应文件（侧载文件、重打包、格式转换后的版本）。可**绑定**到某本书、**重新匹配**（把书放进图书目录并扫描后再试）或**忽略** |
| 同一本书的 EPUB 版和 PDF 版进度不互通 | 协议只认客户端对**文件内容**算出的指纹（partial MD5），不同格式/不同文件必然不同——跨格式续读在协议层不可实现（`doc/m3-sync/task/05-延后项.md` §5.9） |
| 把「Document matching method」改成 Filename 后进度全变孤儿 | 该模式把文档指纹换成 `md5(文件名)`，与已有记录完全不同。**改回 Binary** 即恢复 |
| 在「阅读进度」页"重置进度"后，设备还停在第 100 页 | 重置**只删服务端记录**，设备本地位置不变；设备下次推送会重新建立记录。要让内容真回到开头，得在设备上手动跳转 |
| 想踢掉某台设备 | 协议层没有会话/设备封锁能力（设备列表只读）。要强制所有设备重新登录，只能**改同步口令** |

## 12. 管理端页面（`/admin`）

| 现象 | 原因与处理 |
| --- | --- |
| 打开 `http://主机:18080/` 停在 404 | 这份 jar **没打包管理端产物**（直接 `./mvnw package` 后 `java -jar`，或镜像构建时漏了前端那一步）：`/` 会 302 到 `/admin/`，而那里没有 `index.html`。按 [05](05-Docker部署.md) §1.1 出产物再打包即可；协议服务（`/rest`、`/opds`、KOSync）不受影响 |
| 访问 `/foo` 或拼错的 `/users/creat` 返回 404（不是管理端页面） | **刻意如此**：根命名空间留给协议端点，SPA 兜底只作用于 `/admin/**`。这样客户端拼错路径会明确拿到 404，而不是一页"看起来成功"的 HTML（[ADR-0007](../adr/0007-single-image-admin-under-admin.md)） |
| 刷新 `/admin/books/12` 白屏或 404 | 管理端是 history 模式路由，需要后端把未命中的 `/admin/**` 无扩展名路径兜底到 `index.html`（1.0.0 起已实现）。白屏说明产物 base 不对（必须是 `vite build --base=/admin/`）；404 说明产物没打包进去 |
| 上传封面报"上传文件过大（封面最大 5MB）" | 封面单文件上限 5MB（`spring.servlet.multipart.max-file-size`）。1.0.0 之前这里会误报 500 + code 1200，现已修正为明确的参数错误 |
| 往媒体目录丢的新文件没有自动进库 | 定时扫描默认每天 **03:00**（`bifrost.scan.cron`，6 段含秒；容器时区由 `TZ` 决定，compose 已设 `Asia/Shanghai`）。想立刻生效就在管理端点一次「扫描」；`cron` 留空则关闭定时扫描 |

## 13. 还想看更多细节？

| 需求 | 文档 |
| --- | --- |
| 端点能力矩阵（哪些能用、哪些未实现） | 《音乐管理功能说明》§8.2 |
| 客户端兼容矩阵（目标验证客户端） | 《音乐管理功能说明》§8.4 |
| 协议认证公式、错误码表 | 《Subsonic_API_参考》§2、§3 |
| 配置键完整表 | 《整体技术架构》§6 /《音乐管理技术设计》§10 |
| 电子书 OPDS 端点路径 / 能力边界 | [04-连接阅读器客户端](04-连接阅读器客户端.md) §3/§6、`doc/m2-book/task/02-OPDS发布.md` |
| 阅读进度同步的协议细节与客户端硬约束 | `doc/m3-sync/调研/01-KOSync协议实证.md` |
| 单镜像的构建/推送/部署命令清单 | [05-Docker部署](05-Docker部署.md) |
| 管理端为什么挂 `/admin/`、为什么镜像只打包不构建 | [ADR-0007](../adr/0007-single-image-admin-under-admin.md) |
