# Bifrost 整体技术架构

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v2.0 |
| 更新日期 | 2026-08-14 |
| 文档状态 | 定稿 |
| 前身版本 | v1.1（2026-08-14，本版升级） |

---

## 0. 修订记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.1 | 2026-08-14 | 多模块骨架、技术栈基线、依赖方向规则 |
| **v2.0** | 2026-08-14 | ① 引入持久化（SQLite + Spring Data JPA）；② 新增音乐领域与 Subsonic 服务（模块 `syrinx/subsonic-api`，原 `sonic-client` 移除）；③ 新增统一认证体系；④ 新增配置体系与 Docker 部署；⑤ 文档改名《整体技术架构》，通用内容并入，音乐实现细节拆分至《音乐管理技术设计》 |

---

## 1. 项目概述

家庭媒体库管理平台，统一管理三类媒体资源：

| 媒体类型 | 核心能力 | 所在包 | 状态 |
| --- | --- | --- | --- |
| 音频 audio（音乐） | 扫描、标签解析、聚合索引、Subsonic 服务 | `com.bifrost.core.audio` | ✅ 本轮交付 |
| 视频 video | 媒体扫描、索引 | `com.bifrost.core.video` | 后续 |
| 电子书 book | 元数据解析 | `com.bifrost.core.book` | 后续 |

对外能力：**Bifrost 自身提供 Subsonic 兼容协议服务（Syrinx，`/rest/**`）**，客户端直连；管理 REST（`/api/**`）供 Vue Dashboard（另一项目）调用；Jellyfin 推送、OPDS 图书流为后续预留。

## 2. 技术栈（v2 更新）

| 项 | 选择 |
| --- | --- |
| 语言 | Java 21（编译目标 `maven.compiler.release=21`） |
| 框架 | Spring Boot 3.5.16（Spring Framework 6.2.19） |
| 构建 | Maven 多模块，父工程 `spring-boot-starter-parent` 3.5.16 |
| 坐标 | `com.bifrost:bifrost:1.0.0-SNAPSHOT`，基础包名 `com.bifrost` |
| 工程化 | Lombok（父 POM 统一，optional + annotationProcessorPaths）、Maven Wrapper 3.9.9 |
| Web | `spring-boot-starter-web`（bifrost-api、syrinx/subsonic-api） |
| **持久化（新增）** | `spring-boot-starter-data-jpa` + `org.xerial:sqlite-jdbc` + `org.hibernate.orm:hibernate-community-dialects`（SQLite dialect） |
| **标签解析（新增）** | `net.jthink:jaudiotagger:3.0.1`（bifrost-core/audio；ADR-0002：文档原定 2.0.3 不存在于 Maven Central） |
| **拼音（新增）** | `com.github.stuxuhai:jpinyin:1.1.8`（索引分组） |
| **XML 序列化（新增）** | `com.fasterxml.jackson.dataformat:jackson-dataformat-xml`（Subsonic XML 输出，版本随 Spring BOM） |
| 明确不引入 | JPA 之外：Redis / MyBatis / WebFlux / Actuator |

> 版本说明：第三方版本优先由 Spring Boot BOM 管理；jaudiotagger/jpinyin 需显式固定版本。

## 3. 模块架构

```
bifrost (父级 POM，统一版本)
├── bifrost-common        # 工具包：字符串、日期、文件 IO、自定义异常（纯 Java，无 Spring）
├── bifrost-domain        # 实体（Entity）、DTO、枚举、Repository 接口
├── bifrost-core          # 核心业务（Service 层），包隔离 video / audio / book
├── bifrost-adapter       # 对接外部客户端（聚合 POM）
│   ├── syrinx            # 【音乐网关】聚合模块
│   │   └── subsonic-api  # 【本轮实现】Subsonic 协议服务端（/rest/**，MVC Controller）
│   ├── jellyfin-client   # 占位（视频配套，待定）
│   └── opds-publisher    # 占位（图书 OPDS 流，待定）
├── bifrost-api           # 管理 RESTful Controller 层（/api/**，供 Vue Dashboard 调用）
└── bifrost-bootstrap     # 启动类 + application.yml，生成可执行 jar
```

> 变更说明：原 `sonic-client`（向外部 Sonic 推送）**移除**——产品方向反转，Bifrost 自身即 Subsonic 服务端，音乐网关改名 **syrinx** 并内含 `subsonic-api`。

**依赖关系（单向，禁止反向/循环）**：

```
bifrost-common ◄── bifrost-domain ◄── bifrost-core ◄── bifrost-adapter（聚合，子模块继承）
bifrost-core ◄── bifrost-api ◄── bifrost-bootstrap
bifrost-core ◄── bifrost-adapter/syrinx/subsonic-api ◄── bifrost-bootstrap
```

**包约定**：基础包 `com.bifrost.*`；core 内 `com.bifrost.core.{video,audio,book}` 包隔离；adapter 子模块 `com.bifrost.adapter.{syrinx,jellyfin,opds}`，其中 `com.bifrost.adapter.syrinx.subsonic` 承载 Subsonic 实现。

## 4. 分层与依赖规则

| 规则 | 说明 |
| --- | --- |
| 依赖单向 | common ← domain ← core ← adapter；core ← api ← bootstrap |
| common 无 Spring | 纯 Java 工具与异常，禁止 Spring 依赖 |
| core 无 Web/持久化 | 仅 `spring-boot-starter`；扫描/解析 Service 为普通 `@Service`；Repository 接口在 domain，JPA 实现在各模块运行时装配 |
| subsonic-api 位置 | 位于 adapter 层（用户决策），可引入 `spring-boot-starter-web`；控制器由 bootstrap 跨模块扫描注册（`scanBasePackages="com.bifrost"`） |
| 版本统一 | 内部模块父 POM dependencyManagement 声明 `${project.version}`，子模块不写版本号 |
| 打包 | 仅 bifrost-bootstrap 配置 spring-boot-maven-plugin 生成可执行 fat jar |

## 5. 存储设计

| 项 | 设计 |
| --- | --- |
| 数据库 | SQLite 单文件（`bifrost.db.path`，默认 `./data/bifrost.db`） |
| ORM | Spring Data JPA（Hibernate）+ `hibernate-community-dialects` 的 SQLiteDialect |
| Schema | v1 用 `ddl-auto=update`；后续引入迁移工具（可增强） |
| SQLite 运行参数 | 启用 WAL、busy_timeout，避免读写互斥 |
| 事务 | 边界在 core Service（`@Transactional`）；扫描按文件批次提交 |
| 备份 | 停止写操作后直接复制 db 文件；Docker 场景数据目录挂载宿主卷 |
| 领域模型 | Artist / Album / Track / LibraryRoot / User / Playlist / PlaylistEntry（字段级设计见《音乐管理技术设计》第 1 章） |

## 6. 配置体系

```yaml
bifrost:
  db:
    path: ./data/bifrost.db          # SQLite 文件路径
  library:
    roots:                           # 库根列表（音乐/图书共用，mediaType 区分）
      - name: 我的音乐
        path: D:/Music
        enabled: true
  media:
    cover-cache-dir: ./data/covers   # 封面缩略图缓存
  scan:
    cron: "0 3 * * *"                # 定时扫描 cron（已声明但未接线：无调度实现）
  auth:
    initial-username: admin          # 初始管理员用户名
    # initial-password: 仅环境变量 BIFROST_AUTH_INITIAL_PASSWORD 注入
    # secret: 环境变量 BIFROST_AUTH_SECRET（口令加密主密钥；缺失自动生成落 data/secret.key）
  subsonic:
    enabled: true
    api-version: 1.16.1              # 对外宣称的协议版本
  cors:
    allowed-origins:                  # 跨域来源白名单；空列表=禁用 CORS，默认 *（所有来源）
      - "*"

spring:
  jpa:
    hibernate.ddl-auto: update
  datasource:                        # 由自定义 DataSource 配置指向 SQLite 文件
    url: jdbc:sqlite:./data/bifrost.db
```

覆盖机制：环境变量 / 启动参数 > `SPRING_CONFIG_ADDITIONAL_LOCATION` 外部 yml > 内置 application.yml（详见《通用功能说明》第 2 章）。

## 7. 认证实现要点

| 项 | 设计 |
| --- | --- |
| 账号 | v1 单管理员（User 表预留多用户） |
| 口令存储 | **AES-GCM 可逆加密**（主密钥：环境变量 `BIFROST_AUTH_SECRET`，缺失自动生成落 `data/secret.key` 并警告）——因 Subsonic 令牌 `md5(password+salt)` 验证必须还原明文（见《Subsonic_API_参考》2.2） |
| Subsonic 认证 | `t/s` 令牌（解密后计算 `md5(password+salt)`，恒定时间比较）或 `p` 密码；失败 `200 + status=failed + error 40` |
| 管理 REST 认证 | HTTP Basic 或同款 t/s 令牌；失败 `401 + 信封 code=1002` |
| 安全建议 | 对外走 HTTPS（反代）；`p=enc:` 仅编码非加密 |

## 8. Subsonic 服务技术要点（Syrinx）

| 项 | 设计 |
| --- | --- |
| 挂载 | `/rest/<method>.view`（`@RestController` 映射，`@RequestMapping("/rest")`） |
| 双格式 | XML 默认 + `f=json`（jackson-dataformat-xml；JAXB 不引入） |
| 信封 | `subsonic-response`：status/version/type/serverVersion/openSubsonic（详见 API 参考第 3 章） |
| 认证拦截 | 解析 `u/t/s/p/v/c/f`，校验后放行；`getOpenSubsonicExtensions` 免认证 |
| 流式传输 | `stream/download` 支持 **HTTP Range/206**（`InputStreamResource` + 手动 Range 解析或 `ResourceHttpRequestHandler`）；`Accept-Ranges: bytes`；`X-Content-Type-Options: nosniff` |
| 错误 | 协议错误码 0/10/20/30/40/41/42/43/44/50/60/70（**无 80–99**，见 API 参考 3.2） |
| ID | `ar-/al-/tr-/pl-` + 数字主键 |
| 模拟目录树 | `getMusicDirectory` 按 `/艺术家/专辑/01 - 标题.mp3` 虚拟目录返回（兼容 DSub 系） |
| 缓存失效 | 订阅 `MusicScanCompletedEvent` 刷新（缩略图、nowPlaying 等进程内缓存） |
| 能力通告 | 无 `X-OpenSubsonic` 请求头（该头为讹传，不支持）；用响应字段 + `getOpenSubsonicExtensions` 通告 |

## 9. 构建与运行

```bash
./mvnw clean verify                                  # 全量构建
./mvnw -pl bifrost-bootstrap -am spring-boot:run     # 开发启动，端口 8080
```

验证：`curl http://localhost:8080/api/ping` → `pong`；`curl "http://localhost:8080/rest/ping.view?u=admin&t=...&s=...&v=1.16.1&c=test"` → ok。

### 9.1 Docker 部署（v1）

`docker/Dockerfile`（多阶段）：

```dockerfile
# build: maven:3.9-eclipse-temurin-21 → ./mvnw clean package
# run:   eclipse-temurin:21-jre，非 root 用户，WORKDIR /app
# 拷贝 fat jar，EXPOSE 8080，VOLUME /data
# ENTRYPOINT ["java","-jar","/app/bifrost.jar"]
```

`docker/docker-compose.yml`：

```yaml
services:
  bifrost:
    build: .
    ports: ["8080:8080"]
    volumes:
      - ./data:/data                 # SQLite + 封面缓存 + secret.key 全部在 /data，宿主直接备份
    environment:
      BIFROST_AUTH_INITIAL_PASSWORD: ${BIFROST_AUTH_INITIAL_PASSWORD:?必填}
      BIFROST_AUTH_SECRET: ${BIFROST_AUTH_SECRET:-}
      SPRING_CONFIG_ADDITIONAL_LOCATION: /data/application.yml   # 用户可编辑覆盖配置
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/ping"]
      interval: 30s
      timeout: 3s
      retries: 3
```

- 数据备份：备份宿主 `./data` 目录即完成（含 db、封面缓存、secret.key）。
- 注意：后续里程碑会把管理端页面（另一项目产物）**打包进镜像**，部署形态届时调整。
- Windows 宿主路径：`./data` 相对路径由 compose 解析；或写绝对路径 `C:\path\to\data:/data`。

## 10. 环境与约束

| 项 | 说明 |
| --- | --- |
| JDK | 本机 JAVA_HOME = JDK 22 GraalVM CE 22.0.1，编译目标 release=21，兼容 Java 21 运行时 |
| Maven | 系统 3.6.3（`<blocked>` 标签告警不影响构建；推荐统一用 mvnw 3.9.9） |
| 镜像/仓库 | 内网 datian + 阿里云 public；本地仓库 `E:\newrepo` |
| 编码 | UTF-8 |
| 新增依赖注意 | jaudiotagger/jpinyin 版本显式固定；SQLite 相关依赖需确认内网仓库可达 |

---

## 附录：v1.1 → v2.0 变更对照

| 主题 | v1.1 | v2.0 |
| --- | --- | --- |
| 持久化 | 无 | SQLite + Spring Data JPA |
| 音乐 | 仅包结构 | 完整领域 + 扫描 + 标签解析 + 拼音索引 |
| Sonic | 向外部 Sonic 推送（占位） | 自身 Subsonic 服务（syrinx/subsonic-api） |
| 认证 | 无 | 统一账号（AES-GCM 口令 + Subsonic 令牌） |
| 配置 | 仅 name/port | bifrost.* 配置体系 + 环境变量 + /data 覆盖 |
| 部署 | 仅 mvnw 运行 | + Docker（compose + 卷挂载 + healthcheck） |
| 文档 | 《技术架构.md》 | 改名《整体技术架构.md》，通用内容并入；音乐细节拆分独立文档 |
