# Bifrost — Agent 约定

## Agent skills

### Issue 追踪

issue 与 spec 以 markdown 文件存放在本仓库 `.scratch/<feature>/` 下。见 `docs/agents/issue-tracker.md`。

### Triage 标签

沿用五个默认角色标签（`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`）。见 `docs/agents/triage-labels.md`。

### 领域文档

单上下文：根 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
